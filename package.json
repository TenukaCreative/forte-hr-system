{
  "name": "forte-hr-system",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "start": "cd backend && npm install && node server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
